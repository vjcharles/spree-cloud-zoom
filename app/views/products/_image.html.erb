<% if image %> 
  <%= link_to(image_tag(image.attachment.url(:product), :class => 'cloud-zoom'), image.attachment.url(:original)) %>

<% else %>
  <%= link_to(product_image(@product), "HEY") %>
<% end %>